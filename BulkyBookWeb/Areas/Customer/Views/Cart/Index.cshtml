@model BulkyBook.Models.ViewModels.ShoppingCartVM

<form method="post">
	<br />
	<div class="container">
		<div class="card">
			<div class="card-header bg-dark text-light ms-0">
				<div class="row">
					<div class="col-6 pt-3">
						<i class="fa fa-shopping-cart"></i> &nbsp;
						Shopping Cart
					</div>
					<div class="col-6 text-end">
						<a asp-area="Customer" asp-controller="Home" asp-action="Index" class="btn btn-outline-info btn4 btn">Continue Shopping</a>
					</div>
				</div>
			</div>

			<div class="card-body">
				@foreach(var item in Model.ListCart){
				<div class="row">
					<div class="d-none d-lg-block col-lg-2 text-center py-2">
						<img src="@item.Product.ImageUrl" class="rounded" width="100%" />
					</div>
					<div class="col-12 col-lg-6 pt-md-3">
						<h5><strong>@item.Product.Title</strong></h5>
						<p><small>@Html.Raw(item.Product.ShortenDescription(charCount: 450))</small></p>
					</div>
					<div class="col-12  col-lg-4 text-center row">
						<div class="col-5 text-md-right pt-4">
							<h5><strong>@item.Price.ToString("c")
								<span class="text-muted">&nbsp;x&nbsp;</span>@item.Count </strong></h5>
						</div>
						<div class="col-6 col-sm-4 col-lg-7 pt-2">
							<div class="w-75 btn-group" role="group">
								<a asp-action="plus" asp-route-cartId="@item.Id" class="btn btn4 alert-success">
									@*<i class="bi bi-plus-square"></i>*@
									<i class="fa-solid fa-lg fa-plus"></i>
								</a> &nbsp;
								<a asp-action="minus" asp-route-cartId="@item.Id" class="btn btn4 alert-primary">
									<i class="fa-solid fa-lg fa-minus"></i>
								</a>&nbsp;
								<a asp-action="remove" asp-route-cartId="@item.Id" class="btn btn4 btn-danger">
								<i class="bi fa-lg bi-trash-fill"></i>
							</a>
							</div>

						</div>
						
					</div>
				</div>
				<hr />
				}
				<div class="row">
					<div class="col-12 col-md-6 offset-md-6 col-lg-4 offset-lg-8 pr-4">
						<ul class="list-group">
							<li class="list-group-item d-flex justify-content-between bg-light">
								<span class="text-info"> Total (USD)</span>
								<strong class="text-info">@Model.OrderHeader.OrderTotal.ToString("c")</strong>
							</li>
						</ul>
					</div>
				</div>

			</div>

			<div class="card-footer">
				<div class="card-footer row">
					<div class="col-sm-12 col-lg-4 col-md-6 offset-lg-8 offset-md-6 ">
						<a asp-action="Summary" class="btn btn4 btn-warning form-control">Summary</a>
					</div>
				</div>
			</div>
		</div>
	</div>
</form>